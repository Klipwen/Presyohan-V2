<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:fitsSystemWindows="true">

    <!-- Header bar -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layoutHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:paddingBottom="10dp"
        android:elevation="2dp"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/headerTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:layout_marginEnd="8dp"
            android:layout_marginStart="8dp"
            android:background="@drawable/bg_header_yellow"
            android:gravity="center"
            android:paddingVertical="8dp"
            android:text="Notifications"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/btnBack"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/icon_back"
            android:layout_marginStart="16dp"
            app:layout_constraintBottom_toBottomOf="@+id/headerTitle"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/headerTitle"
            app:tint="@color/white"
            android:contentDescription="Back" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Curved Header Container -->
    <LinearLayout
        android:id="@+id/curvedHeaderContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_category_filter"
        android:orientation="vertical"
        android:elevation="6dp"
        android:paddingBottom="16dp"
        app:layout_constraintTop_toBottomOf="@id/layoutHeader">

        <!-- Filter Tabs (Segmented Control Layout with sliding indicator) -->
        <FrameLayout
            android:id="@+id/tabContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="16dp"
            android:layout_marginTop="4dp"
            android:background="@drawable/bg_segmented_control"
            android:padding="3dp">

            <!-- Sliding Selection Indicator View -->
            <View
                android:id="@+id/tabIndicator"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:background="@drawable/bg_segmented_tab_selected" />

            <!-- Text Tabs Container -->
            <LinearLayout
                android:id="@+id/tabsLinearLayout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <!-- Tab: All -->
                <TextView
                    android:id="@+id/chipAll"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:gravity="center"
                    android:paddingVertical="8dp"
                    android:text="All"
                    android:textColor="@color/presyo_orange"
                    android:textSize="12sp"
                    android:textStyle="bold" />

                <!-- Tab: Join request -->
                <TextView
                    android:id="@+id/chipJoin"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1.2"
                    android:gravity="center"
                    android:paddingVertical="8dp"
                    android:text="Join request"
                    android:textColor="@color/edittext_hint"
                    android:textSize="12sp" />

                <!-- Tab: Store Invites -->
                <TextView
                    android:id="@+id/chipInvites"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1.2"
                    android:gravity="center"
                    android:paddingVertical="8dp"
                    android:text="Store Invites"
                    android:textColor="@color/edittext_hint"
                    android:textSize="12sp" />

                <!-- Tab: Suki -->
                <TextView
                    android:id="@+id/chipSuki"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1.0"
                    android:gravity="center"
                    android:paddingVertical="8dp"
                    android:text="Suki"
                    android:textColor="@color/edittext_hint"
                    android:textSize="12sp" />

                <!-- Tab: System -->
                <TextView
                    android:id="@+id/chipSystem"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1.0"
                    android:gravity="center"
                    android:paddingVertical="8dp"
                    android:text="System"
                    android:textColor="@color/edittext_hint"
                    android:textSize="12sp" />

            </LinearLayout>

        </FrameLayout>

    </LinearLayout>

    <!-- Notifications RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewNotifications"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:paddingTop="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/curvedHeaderContainer" />

</androidx.constraintlayout.widget.ConstraintLayout>
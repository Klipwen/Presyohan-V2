# Tasks

- [x] Create drawable for teal search edittext background
- [x] Create layout for internet search card (`item_internet_search_card.xml`)
- [x] Create layout for internet product list item (`item_internet_product.xml`)
- [x] Restructure search bar and add header in `activity_customer_home.xml`
- [x] Update `GeminiParser.kt` with serialization models and search method
- [x] Update `CustomerHomeActivity.kt` with view initialization, listeners, adapter binding, and mode toggles
- [x] Create layout for "Load More Prices" card (`item_load_more_prices.xml`) with text, icon, and loading state progress bar
- [x] Implement `SearchItem.LoadMorePrices` model variant and update `CustomerSearchAdapter`
- [x] Implement `triggerLoadMorePrices` coroutine logic with 800ms delay in `CustomerHomeActivity.kt`
- [x] Add randomization: shuffle `baseProducts` list on load and shuffle categories on store cards
- [x] Replace `icon_globe` drawable with `icon_internet` across layout XML files
- [x] Position `InternetSearchCard` at the absolute bottom of both default and search result lists, showing a thin divider line above it
- [x] Create toggle switch color selectors `switch_thumb_color.xml` and `switch_track_color.xml` in `res/color`
- [x] Update settings layout `activity_settings.xml` with "Internet Price Search" settings card and switch toggle
- [x] Update `SettingsActivity.kt` to load/save preference and toggle visibility based on portal side
- [x] Update `CustomerHomeActivity.kt` to check toggle preference in `onResume`, focus listener, and list rendering
- [x] Ensure internet icon is hidden from search bar on Stores tab, and internet card is not added on Stores tab list
- [x] Ensure search bar expands to full-scale length matching the Stores tab search bar when the setting is toggled off
- [x] Group internet search back arrow, internet icon, and header title horizontally centered in `activity_customer_home.xml`
- [x] Do not hide the top `headerPresyohan` (brand logo, bell, avatar) in Internet Price Search mode
- [x] Let the internet icon appear on the Prices tab whenever the search query is empty (no letter inputted)
- [x] Remove the "Load More Prices" pagination button and list limit when the user is searching (query is not empty)
- [x] Reduce vertical margins and paddings around the brand logo and internet search headers for a compact spacing layout
- [x] Remove the raw API error Toast from the Gemini search catch block
- [x] Add a local `isGibberish` check to detect query keyboard mash instantly and show the empty state without long waiting times
- [x] Remove the duplicate status bar inset listener applied to `layoutInternetHeader` to resolve the double status bar padding layout spacing bug
- [x] Fix the refresh bug to prevent local database items from replacing internet search results during pull-to-refresh or activity resumes
- [x] Add a dynamic top margin of 12dp to the internet search card when it appears as the first item in the list (e.g., when no local items are found)
- [x] Create vector drawable `icon_searching.xml` (standard high-quality searching icon)
- [x] Create vector drawable `icon_internet_slash.xml` (slashed internet globe icon)
- [x] Redesign layout in `activity_customer_home.xml` to match the new inline internet search header and same header height
- [x] Add a horizontal divider `headerDivider` below the brand header visible only in internet search mode
- [x] Add `ivSearchInternetIcon` (inside input on left) and `btnInsideSearchAction` (inside search bar on right) in `activity_customer_home.xml`
- [x] Add `< Back to local stores` top button (`btnBackToLocalStoresTop`) directly below curvedHeaderContainer
- [x] Add horizontal layout action card `layoutInternetBottomCard` for empty / offline bottom states
- [x] Add `item_back_to_local_stores.xml` layout and support `BackToLocalCard` in `CustomerSearchAdapter` list rendering
- [x] Bind view actions, clicks, animations, retry template dialogs, and exit mode confirmations in `CustomerHomeActivity.kt`
- [x] Verify execution and compilation
